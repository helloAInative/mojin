# 贡献指南

感谢你改进 Mojin。项目仍处于早期阶段，提交应尽量小、可验证，并保持模拟交易与真实交易之间的明确边界。

## 开始开发

1. Fork 仓库并从 `main` 创建分支。
2. 安装 Rust 1.85 或更高版本。
3. 在 `mj-server` 目录执行 `cargo test`。
4. 使用 `./scripts/dev-up.sh --fg` 启动服务，通过 `/healthz` 检查运行状态。

## 提交要求

- 运行 `cargo fmt --check` 和 `cargo test`。
- 修复缺陷时添加能够重现问题的测试。
- 修改 API、数据库结构或运行方式时同步更新文档。
- 不提交 `.env`、API Key、真实账户数据、SQLite 数据库或构建产物。
- 不加入真实券商下单或规避交易平台风控的功能。
- 提交信息使用清晰的祈使句，例如 `Fix paper account reconciliation`。

## Pull Request

PR 描述应包含：问题背景、改动范围、验证方式、兼容性或迁移影响。一个 PR 尽量只解决一个主题。维护者可能要求补测、拆分或修改接口后再合并。

## 数据库变更

当前迁移入口是 `mj-server/migrations/001_init.sql`，同时包含新建数据库结构和向后兼容逻辑。新增字段或表时必须验证：

- 空数据库可以初始化；
- 已有数据库可以重复执行迁移；
- 重复启动不会产生重复初始化数据。

## 报告问题

普通缺陷请使用 GitHub Issue，并提供操作系统、Rust 版本、复现步骤、期望行为和已脱敏日志。安全问题请遵循 [SECURITY.md](SECURITY.md)，不要公开披露。
