# 安全策略

## 支持范围

当前只为 `main` 分支最新版本提供安全修复。项目尚未发布稳定版本。

## 私密报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要为未修复漏洞创建公开 Issue。

报告应包含受影响版本、复现步骤、影响范围、可能的修复方向，以及已经去除密钥、Cookie、个人数据和真实账户信息的日志。维护者会尽快确认报告，并在修复可用后协调披露。

## 部署建议

- 默认保持 `127.0.0.1` 绑定，通过可信私有网络访问。
- 不要将 `.env`、数据库或备份提交到版本库。
- 在反向代理层配置身份验证、TLS、请求大小限制和访问日志脱敏。
- 本项目没有内置用户认证，不应直接暴露到互联网。
- 定期备份 SQLite，并在独立位置验证恢复流程。

行情和 AI 结论错误属于数据质量问题；涉及代码执行、未授权访问、密钥泄露或数据破坏的问题属于安全问题。
