{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Only grant the main desktop window the default Tauri core permissions.",
  "windows": ["main"],
  "platforms": ["macOS", "windows"],
  "permissions": ["core:default"]
}
